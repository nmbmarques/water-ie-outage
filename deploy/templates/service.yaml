apiVersion: v1
kind: Service
metadata:
  name: {{ include "water-ie-outage.fullname" . }}
  labels:
    {{- include "water-ie-outage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "water-ie-outage.selectorLabels" . | nindent 4 }}